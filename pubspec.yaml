name: workshop_flutter_gemma_hybrid_ai
description: "Workshop: Hybrid AI in Flutter — From Cloud to On-Device"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Step 2-3: Cloud AI
  # firebase_core: ^4.4.0
  # firebase_ai: ^3.8.0

  # Step 4: Local AI
  # flutter_gemma: ^0.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/tourist_data/
